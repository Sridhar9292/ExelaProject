<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace='com.exelatech.ecxapi.mapper.ExpectedDatafeedMapper'>
    <resultMap type='com.exelatech.ecxapi.model.ExpectedDatafeed' id='ExpectedDatafeedResult'>
        <id property='feedID' column='FEED_ID'/>
        <result property='feedName' column='FEED_NAME'/>
        <result property='vendorCode' column='VENDOR_CODE'/>
        <result property='filePattern' column='FILE_PATTERN'/>
        <result property='fileType' column='FILE_TYPE'/>
        <result property='expectedTime' column='EXPECTED_TIME'/>
        <result property='fileSchedule' column='FILE_SCHEDULE'/>
        <result property='singleMulti' column='SINGLE_MULTI'/>
        <result property='gracePeriod' column='GRACE_PERIOD'/>
    </resultMap>

    <select id="search" resultMap="ExpectedDatafeedResult">
        SELECT edf.*
        FROM ECX.EXPECTED_DATAFEED edf
        WHERE upper(edf.FEED_NAME) LIKE upper(#{searchTerm})
        OR upper(edf.VENDOR_CODE) LIKE upper(#{searchTerm})
        OR upper(edf.FILE_PATTERN) LIKE upper(#{searchTerm})
        OR upper(edf.FILE_TYPE) LIKE upper(#{searchTerm})
        ORDER BY edf.FEED_ID
    </select>

    <select id="get" resultMap="ExpectedDatafeedResult">
        SELECT edf.*
        FROM ECX.EXPECTED_DATAFEED edf
        WHERE edf.FEED_ID = #{feedID}
        ORDER BY edf.FEED_ID
    </select>

    <select id="getAll" resultMap="ExpectedDatafeedResult">
        SELECT edf.*
        FROM ECX.EXPECTED_DATAFEED edf
        ORDER BY edf.FEED_ID
    </select>

    <select id="exists" resultMap="ExpectedDatafeedResult">
        SELECT count(1) as count
        from ECX.EXPECTED_DATAFEED
        WHERE FEED_ID = #{feedID}
    </select>

    <insert id="insert" parameterType="com.exelatech.ecxapi.model.ExpectedDatafeed">
        INSERT
        INTO ECX.EXPECTED_DATAFEED
            (
                FEED_ID,
                FEED_NAME,
                VENDOR_CODE,
                FILE_PATTERN,
                FILE_TYPE,
                EXPECTED_TIME,
                FILE_SCHEDULE,
                SINGLE_MULTI,
                GRACE_PERIOD
            )
            VALUES
            (
                #{feedID ,jdbcType=VARCHAR},
                #{feedName ,jdbcType=VARCHAR},
                #{vendorCode ,jdbcType=VARCHAR},
                #{filePattern ,jdbcType=VARCHAR },
                #{fileType ,jdbcType=VARCHAR},
                #{expectedTime ,jdbcType=VARCHAR},
                #{fileSchedule ,jdbcType=VARCHAR},
                #{singleMulti ,jdbcType=VARCHAR},
                #{gracePeriod ,jdbcType=VARCHAR}
            )
    </insert>

    <insert id="update" parameterType="com.exelatech.ecxapi.model.ExpectedDatafeed">
        UPDATE ECX.EXPECTED_DATAFEED
        SET
            FEED_NAME=#{feedName ,jdbcType=VARCHAR},
            VENDOR_CODE=#{vendorCode ,jdbcType=VARCHAR},
            FILE_PATTERN=#{filePattern ,jdbcType=VARCHAR},
            FILE_TYPE=#{fileType ,jdbcType=VARCHAR},
            EXPECTED_TIME=#{expectedTime ,jdbcType=VARCHAR},
            FILE_SCHEDULE=#{fileSchedule ,jdbcType=VARCHAR},
            SINGLE_MULTI=#{singleMulti ,jdbcType=VARCHAR},
            GRACE_PERIOD=#{gracePeriod ,jdbcType=VARCHAR}
        WHERE
            FEED_ID=#{feedID}
    </insert>

    <delete id="remove" parameterType="com.exelatech.ecxapi.model.ExpectedDatafeed">
        DELETE FROM ECX.EXPECTED_DATAFEED
        WHERE FEED_ID=#{feedID}
    </delete>
</mapper>
